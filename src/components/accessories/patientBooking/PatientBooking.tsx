import React, { FC } from "react";
import BookingForm from "./bookingForm/BookingForm";
import "./styles.scss";

const PatientBooking: FC = (props) => {
  return <BookingForm />;
};

export default PatientBooking;
