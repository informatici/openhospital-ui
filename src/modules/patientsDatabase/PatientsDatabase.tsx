import * as React from "react";

class PatientsDatabase extends React.Component {
    public render() {
        return (
            <div>
                sono la pagina di PatientsDatabase
        </div>
        );
    }
}

export default PatientsDatabase;